package com.example.todoapp;

public class WorkTask extends Task {
    WorkTask(String title, String description, myDOB dueDate, String status) {
        super(title, description, dueDate, status);
    }
}
